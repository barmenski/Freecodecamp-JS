function sumFibs(num) {
  let result=[];
  let temp=0;
  let sum=0;
  let n=1;
  let sumF=0;
  while (n<num) {
    temp=sum+n;
    n=sum;
    sum=temp;
   
   if (temp%2!=0) {
     result.push(temp);
     sumF=sumF+temp;
   };
  }
 
  console.log(result);
  return sumF;
}
console.log(sumFibs(400))
