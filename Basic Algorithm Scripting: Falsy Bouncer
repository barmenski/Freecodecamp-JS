function bouncer(arr) {
let arrA=[];
for (let i=0; i<=arr.length-1; i++) {
  if (Boolean(arr[i])===false) {
    i++;
  } else {
    arrA.push(arr[i]);
  }

}
  return arrA;
}

bouncer([7, "ate", "", false, 9]);
